public class Principal {

    // Método principal
    public static void main(String[] args) {
        Ventana v = new Ventana();  // Instanciar un objeto de la clase Ventana

        // Por defecto, las ventanas son invisibles, así que la hacemos visible
        v.setVisible(true);  // Hacer visible la ventana
    }
}